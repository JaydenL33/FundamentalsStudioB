var foo; 
if (7*3 >= 21) { 

	foo = 21;

} else {

	foo = -21;

}

console.log(foo);

/* This is a multi-line
Comment
I know it's a little odd, but it's like C
*/

// This is just single line

var obj = {

	firstname: "Jane",
	// Make sure put in the Comma, otherwise the object will be wrong. Whoops. It is literally a Javascript Object/JSON.
	lastname: "Doe"
};

obj.age = 123;

console.log(obj.age);
